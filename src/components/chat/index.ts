/**
 * 채팅 UI — 패널/메시지/입력·헬퍼는 이 폴더 플랫 레이아웃.
 * 타입 `@/types/chat`, 데이터 `@/hooks/useChatMessages`, 읽음 `@/hooks/useChatMessageRead`, 패널 크롬 `@/hooks/useChat`·`chat-panel-store`.
 */

export { ChatPanel } from "./ChatPanel";
